/*using UnityEngine;
using System.Collections;
//including some .NET for dynamic arrays called List in C#
using System.Collections.Generic;

[System.Serializable]

This class will be used alongside Quadrant
to make the map into a searchable datastructure 

public class Grid
{
	// Attributes and such
	float width, height; // of the total Grid
	float nRows, nCols; // Determined by the size
	float wRow, hRow;

	private ArrayList<Quadrant> quads;

	//Each grid is made up of Quadrants. Think rows, columns, and cells.

	// Use this for initialization. Rows and columns created using width and height
	void Start (float w_, float h_) {

		quads = new ArrayList<Quadrant>(); // instantiate new ArrayList of quads

		Debug.Log("Grid created.");

	}

	void InstantiateGrid()
	{
		// for now, let's just make it 10x10 to make things easy
		for(float i = 0; i < 10; i++)
		{
			// rows
			for(float j = 0; j < 10; j++)
			{
				// columns. eg, (i,j)
				//quads.Add(new Quadrant(

			}
		}
	}
	
	// Update is called once per frame
	void Update () {
		
	}
	
	
}*/

